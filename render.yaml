services:
  - type: web
    name: transaction-status-agent
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn transaction_status_agent.testing.web_ui.server:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: ENV
        value: production
